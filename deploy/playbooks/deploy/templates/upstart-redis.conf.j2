description "Redis server"

start on {{ upstart_start_on }}
stop on shutdown

setuid {{ login_user }}

script
  chdir {{ django_path }}
  . {{ django_path }}/.env
  exec {{ venv_path }}/bin/celery beat -A config
end script
